return {
	"kosayoda/nvim-lightbulb",
	event = "InsertEnter",
	config = function()
		require("nvim-lightbulb").setup {
			sign = true,
			virtual_text = true,
			debounce = 150,
			sign_priority = 40,
			virtual_text_priority = 50,
		}
	end,
}
